language: csharp
mono: none
sudo: required
dist: xenial
dotnet: 2.2
before_script:
  - export PATH="$PATH:$HOME/.dotnet/tools"
script:
    - dotnet restore
    - dotnet build 
cache:
  directories:
    - '$HOME/.nuget/packages'
    - '$HOME/.local/share/NuGet/Cache' 
